# 1claw Context Strategy

## 现状结论

这次排查到的核心问题，不是单纯“history 条数不够”，而是上下文来源之间的优先级有缺陷：

1. Flutter 端发送消息时，确实会附带最近一段本地 history。
2. Go 服务端也确实把 `session_id` 和 `history` 继续传给 Hermes wrapper。
3. 但 Python wrapper `hermes_agent.py` 之前的逻辑是：
   - 只要 `session_db` 里有历史，就直接使用它。
   - 只有在 `session_db` 为空时，才回退到客户端传来的近邻 history。

这会导致一个直接问题：

- 如果 `session_db` 中的记录落后、残缺、写入时机不稳定，或者和当前前端本地上下文不完全一致，模型就会优先吃到一份旧上下文。
- 结果表现出来就是：刚聊过的一两轮内容，前端明明还有，但 agent 看起来像“完全失忆”。

补充说明：这次在仓库内没有找到一个明确的“50 条 history”本地配置。当前仓库里可见的实际行为是：

- 前端发送 bootstrap history。
- Hermes wrapper 使用 session memory 和 SessionDB。
- 历史窗口此前主要由前端最近消息和 wrapper 内的 session_db 决定，而不是一个明确的统一 `50` 条配置。

## 这次改动

这次修复做了两件事：

1. 不再让 `session_db` 和客户端近邻 history 二选一。
2. 改为把两者合并，然后裁剪成一个“最近优先”的上下文窗口。

新窗口策略：

- 最近 10 轮用户回合优先。
- 单条消息限制在合理长度内，避免长代码块吞掉整个窗口。
- 总字符预算受控，避免上下文无限膨胀。
- 如果客户端 recent history 与 `session_db` 尾部有重叠，则做重叠消解，避免重复堆积。

这个策略的目的不是追求“把所有历史都塞进去”，而是保证：

- 最近话题不丢。
- 当前问题链条连续。
- 历史长度可控。
- 当持久化历史和本地近邻历史不一致时，最近几轮仍然有机会进入模型上下文。

## 为什么不是简单改成更多条数

单纯把窗口从 20 条、50 条、100 条往上调，属于粗暴放大：

- 好处：短期内可能减少遗漏。
- 问题：旧内容会和新内容竞争预算。
- 问题：越久远的噪声越容易挤掉真正重要的最近上下文。
- 问题：成本更高，延迟更高，且模型注意力也不是无限的。

所以比“固定更多条数”更合理的是：

- 近邻窗口保证连续性。
- 中期记忆做摘要。
- 远期记忆按需检索。

## 推荐的长期方案

建议采用三层上下文：

1. 近邻窗口
   - 直接送进模型。
   - 默认取最近 8 到 12 轮，当前先按 10 轮执行。
   - 保留原始对话，不做激进摘要。

2. 会话摘要
   - 每隔若干轮，把已经“离开近邻窗口”的内容摘要成结构化笔记。
   - 摘要重点不是复述所有聊天，而是提炼：
     - 当前目标
     - 已确认约束
     - 已做结论
     - 未解决问题
     - 用户明确偏好

3. 远期检索
   - 老消息不要永远硬塞进 prompt。
   - 当新问题命中旧主题时，再按关键词、实体或摘要标签取回相关片段。

这三层比固定 `50` 条 history 更稳，因为它把“连续性”和“容量”拆开处理了。

## 对“取 10 轮并抽离关键内容”的判断

这个方向是对的，但要拆成两件事：

1. 最近 10 轮：直接保留原文。
2. 更早内容：抽摘要，而不是继续保留原文。

如果把“最近 10 轮”也先摘要再发，有两个风险：

- 丢语气和措辞细节。
- 丢掉一些模型实际很依赖的局部上下文，比如刚刚那一轮里的限定条件、引用代码、反问、否定条件。

所以更好的做法不是“10 轮并且全部抽离关键内容”，而是：

- 最近 10 轮保原文。
- 10 轮之外抽摘要。

## 市场上常见做法

公开产品通常不会只靠“固定 N 条消息”吃完整个上下文。虽然具体实现是私有的，但从产品表现和业界通用方案看，基本都是混合策略：

1. 当前窗口直接带最近若干轮原始消息。
2. 更早的历史做滚动摘要。
3. 命中旧话题时，按需把更早内容检索回来。
4. 对文件、网页、代码块这类长内容，会单独建索引或附件上下文，而不是简单混在聊天历史里。

ChatGPT、Kimi 这类网页版的体感通常比“固定 50 条”稳定，原因一般就在这里：

- 最近几轮连续性更强。
- 老信息不是完全丢失，而是以摘要或检索形式回流。
- 长会话里会尽量把“主题状态”保存下来，而不是只记字面消息条数。

## 下一步建议

如果继续做上下文质量提升，建议按这个顺序推进：

1. 先观察这次修复后，“刚聊过却失忆”的问题是否明显下降。
2. 再增加会话级结构化摘要，而不是先继续盲目加 history 条数。
3. 最后再做按主题的旧消息检索，把远期记忆从 prompt 常驻改成按需召回。

## 本次落地范围

本次已经落地的是第一步：

- 修复 `session_db` 覆盖近邻 history 的问题。
- 改成近邻优先、带预算的上下文窗口。

这能先把“完全失忆”的最明显问题压下去。